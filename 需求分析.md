# 小学生打字练习网站 — 需求分析（基于 Cloudflare）· 决策锁定版

> 文档版本：v1.1 ｜ 日期：2026-07-27 ｜ 状态：需求已对齐，待开发
> 目标：在 Cloudflare 上做一个给小学生练英文打字的 Web 应用，本地优先、可选云端、含家长/老师看板。

---

## ✅ 已锁定决策（来自需求对齐）

| 决策项 | 结论 |
|---|---|
| 练习输入类型 | **英文打字**（26 字母 + 标点，标准 WPM）。中文拼音列入二期。 |
| 数据存储模型 | **本地优先**：不登录即可玩，进度默认存浏览器。云端为可选增强。 |
| 首期范围 | **全栈含后台**：首期即规划后端与看板，但分可独立验证的子阶段交付。 |

> 调和说明：「纯本地」≠「不要后端」。本设计采用 **local-first**：学生端零登录、零 PII；后端（D1 + Functions）只承载「匿名排行榜」与「家长/老师聚合看板」两类增值能力，且看板不反过来要求学生注册账号。

---

## 1. 项目背景与目标

### 1.1 背景
打字指法与输入速度是数字时代基础能力，小学是建立肌肉记忆的黄金期。现有产品多面向成人/英文场景，缺少**面向中国小学生、有趣且安全**的练习环境。

### 1.2 目标
- **核心**：帮 6–12 岁学生通过游戏化关卡，从「认识键位」提升到「盲打常用词/句」。
- **体验**：界面可爱、反馈即时、无广告、无社交压力；打开即玩，不需注册。
- **技术**：用 Cloudflare 边缘网络与免费额度，零服务器运维、全球低延迟、成本低。

### 1.3 成功指标（建议）
| 指标 | 说明 | 目标值 |
|---|---|---|
| 单次完成率 | 开始并打完一段的比例 | ≥ 80% |
| 指法正确率提升 | 关卡间准确率环比 | 可观测即可 |
| 家长满意度 | 看板好评 | 定性收集 |

---

## 2. 目标用户与角色

| 角色 | 核心诉求 | 约束 |
|---|---|---|
| **小学生（主用户）** | 好玩、有奖励、看见自己变强 | 注意力短、需大字号/大按键、怕挫败 |
| **家长** | 知道孩子练了什么、有无进步、是否安全 | 关心隐私、怕沉迷、要简单 |
| **老师（可选）** | 布置练习、看班级进度、导出 | 多学生管理、数据导出 |
| **开发者（你）** | 易部署、易维护、低成本 | 免费/低价、少运维 |

---

## 3. 核心功能需求（Functional Requirements）

### 3.1 打字练习引擎（核心，100% 前端）
- 实时捕获 `keydown`/`input`，逐字符比对目标文本，标记正确/错误。
- **指法提示**：高亮当前应使用的手指与键位（左手食指、右手小指等）。
- 指标：速度（WPM）、准确率、错误类型（错/漏/多/退格次数）。
- 即时反馈：对绿错红 + 音效/震动；光标跟随。
- 计时与结算：倒计时或定长文本打完即结算。

### 3.2 课程 / 关卡体系
- 渐进难度：单键位（`asdf jkl;`）→ 字母组合 → 常用单词 → 短句 → 段落。
- 关卡解锁：上一关达标（准确率 ≥ 90%）后解锁下一关。
- 内容分级：按年级提供不同词库（配置文件管理，非硬编码）。

### 3.3 练习语言（已锁定：英文）
- **首期：英文打字**。屏幕给英文文本，学生直接键入，逐字符比对，计算标准 WPM。
- 中文拼音（需处理输入法 `compositionstart/end` + 上屏文本比对）列入**二期**，引擎结构预留扩展位。

### 3.4 游戏化与激励
- 星星 / 勋章 / 连击（combo）；过关动画与音效（可关闭）；
- 个人最佳成绩展示；可选电子宠物伴随成长（打字喂养）。

### 3.5 进度与统计（本地优先）
- 个人进度默认存浏览器（`localStorage` / `IndexedDB`），无需登录；
- 可视化报告：速度曲线、准确率趋势、练习时长；
- 家长可看「本周练习概览」。

### 3.6 全球匿名排行榜（可选增值，需联网）
- 仅用**昵称 + 成绩**上榜，不收集真实身份；
- 提交时加 **Cloudflare Turnstile** 校验，防机器人刷分；
- 频率限制 + 异常分数过滤。

### 3.7 家长 / 老师看板（首期范围，隐私安全模型）
设计原则：**学生端永不登录、不采集 PII**；看板通过「本地生成分享链接 / 班级码」实现，不反向要求学生注册。

- **家长视图**：学生在本地生成一枚「只读报告链接」（含加密 token），分享给家长查看进度与趋势。后端不存学生身份，只存聚合报告。
- **老师视图**：用「班级码（class code）+ 轻量管理员 token」进入，集中看班级进度、布置练习、导出 CSV。**无学生个人账号**。
- **合规底线**：不采集姓名/手机号/人脸；无第三方广告与追踪 SDK；符合 COPPA 与中国《儿童个人信息网络保护规定》。

---

## 4. 非功能需求（NFR）

| 类别 | 要求 |
|---|---|
| 性能 | 首屏 < 1.5s（静态资源走边缘缓存）；输入延迟感知为 0（本地计算）。 |
| 兼容性 | 桌面 Chrome/Edge/Safari 最新版为主；打字强依赖物理键盘，移动端仅看报告。 |
| 可访问性 | 大字号（≥20px）、高对比度、可关声音。 |
| 安全与隐私 | 见 3.7 合规底线；提交接口用 Turnstile。 |
| 成本 | 优先 Cloudflare 免费额度；D1/KV 按量计费，量级极小。 |
| 可维护性 | 词库/关卡用配置文件；部署一条命令完成。 |

---

## 5. Cloudflare 技术架构映射

```
┌─────────────────────────────────────────────┐
│  浏览器（学生/家长/老师）                      │
│  · 静态前端：HTML/CSS/JS（打字引擎·动画·看板）  │
│  · 本地进度：浏览器存储（local-first）         │
└───────────────┬─────────────────────────────┘
                │  HTTPS（全球边缘）
┌───────────────▼─────────────────────────────┐
│  Cloudflare Pages（静态托管 + 边缘缓存）       │
└───────────────┬─────────────────────────────┘
                │  仅「排行榜/看板」需要时
┌───────────────▼─────────────────────────────┐
│  Pages Functions / Worker（轻量 API）         │
│  · 成绩提交（Turnstile 校验）· 报告/班级聚合   │
└───────┬───────────────────────┬─────────────┘
        │                        │
┌───────▼──────┐          ┌──────▼──────┐
│  D1 (SQLite) │          │  KV (缓存)  │
│ 成绩/排行榜   │          │ 词库/配置缓存│
│ 报告/班级聚合 │          │             │
└──────────────┘          └─────────────┘
```

| 能力 | 选用产品 | 理由 |
|---|---|---|
| 前端托管 | **Cloudflare Pages** | 静态边缘分发、免费、Git/Direct Upload 均可 |
| 服务端逻辑 | **Pages Functions / Worker** | 仅排行榜/报告/班级聚合时启用 |
| 结构化数据 | **D1（SQLite）** | 成绩、排行榜、班级聚合天然关系型；免费额度够用 |
| 配置/词库缓存 | **KV** | 词库变更少，缓存友好 |
| 人机校验 | **Turnstile** | 免费、隐私友好，防刷分 |
| 分析（可选） | **Web Analytics** | 隐私友好基础访客统计，零配置 |

> 核心打字交互全在浏览器本地；后端仅在「匿名排行榜 / 家长报告链接 / 老师班级聚合」时介入。

---

## 6. 数据模型（D1 表设计，草案）

```sql
-- 匿名成绩（仅昵称，无 PII）
CREATE TABLE scores (
  id          INTEGER PRIMARY KEY AUTOINCREMENT,
  nickname    TEXT NOT NULL,
  wpm         REAL,
  accuracy    REAL NOT NULL,
  duration_s  INTEGER,
  score       INTEGER,
  created_at  INTEGER NOT NULL,
  region      TEXT
);
CREATE INDEX idx_scores_score ON scores(score DESC);

-- 班级聚合（老师视图，按班级码聚合，不存学生身份）
CREATE TABLE class_reports (
  class_code  TEXT NOT NULL,
  date        TEXT NOT NULL,
  total_min   INTEGER,        -- 班级总练习分钟
  avg_wpm     REAL,
  avg_acc     REAL,
  updated_at  INTEGER NOT NULL,
  PRIMARY KEY (class_code, date)
);

-- 关卡/词库（也可放 KV）
CREATE TABLE lessons (
  id      INTEGER PRIMARY KEY,
  grade   INTEGER,
  level   INTEGER,
  type    TEXT,               -- 'key'|'word'|'sentence'
  content TEXT NOT NULL
);
```

> 学生个人进度默认存浏览器本地，不进 D1；只有「愿意上榜 / 加入班级」的数据才提交（且不含身份）。

---

## 7. 仍需确认的一点（看板访问模型）

决策已锁定「全栈含后台 + 本地优先」。仅剩一个落地细节建议按如下默认执行，如无异议即据此开发：

- **家长报告链接**：学生在本地一键生成只读链接（含加密 token），后端仅存聚合报告，不关联学生身份。
- **老师班级码**：老师建班级得 class code，学生输入班级码即把匿名成绩并入班级聚合；老师用管理员 token 看板。
- 若你希望「家长/老师必须注册账号才能看」，请告知——但这会提高合规与工作量，默认不采用。

---

## 8. 交付里程碑（首期即全栈，分三子阶段）

| 子阶段 | 范围 | Cloudflare 用到 | 可独立验证 |
|---|---|---|---|
| **A. 前端 MVP** | 英文打字引擎 + 关卡 + 游戏化 + 本地进度 | Pages（纯静态） | 打开即玩、可计分 |
| **B. 后端 + 排行榜** | 匿名排行榜 + Turnstile 防刷 + KV 词库 | + Functions + D1 + Turnstile | 可上榜、防刷 |
| **C. 家长/老师看板** | 报告链接 + 班级码 + 聚合看板 + CSV 导出 | + D1 扩展 | 家长/老师可看 |

---

## 9. 风险与对策
| 风险 | 对策 |
|---|---|
| 儿童注意力短、易挫败 | 关卡短、即时奖励、可调难度、可关音效 |
| 排行榜被刷 | Turnstile + 频率限制 + 异常分数过滤 |
| 隐私合规风险 | 不收 PII、无第三方追踪、数据最小化、学生端零登录 |
| 预算失控 | 全程免费额度；D1/KV 按量计费，量级极小 |

---

## 下一步
确认第 7 节看板模型后，我将输出 **技术选型定稿 + Wrangler 配置脚手架 + 开发任务拆解**，并从子阶段 A（纯静态前端 MVP）开始搭建。
